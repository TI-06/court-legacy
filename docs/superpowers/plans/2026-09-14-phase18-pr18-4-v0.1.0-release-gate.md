# Phase18 PR18-4 v0.1.0 Release Gate Implementation Plan

Date: 2026-09-14
Base: `main@2f21c00b40bda57019b322a90e119d7e92aa2c3b`
Branch: `feature/phase18-v0.1.0-release-gate`

## Goal

Make Court Legacy explicitly tag-ready as `v0.1.0` with a reproducible release decision point, release evidence, rollback instructions, and minimal additional GitHub Actions noise.

## Constraints

- Set the authoritative package release version to `0.1.0`.
- Keep persisted save schema exactly v8.
- Do not change gameplay behavior or Phase16 PvP authority/privacy/idempotency behavior.
- Do not restore feature-branch push CI.
- Keep long soak manual/release-only rather than running it on every pull request.
- Reuse the Phase18-2 30-season evidence because PR18-3 and PR18-4 do not change production simulation/domain behavior.
- Keep `package-lock.json` dependency integrity authoritative through `npm ci`; PR18-4 changes no dependency versions.

## Task 1 — Version and release metadata check

**Files**

- Modify: `package.json`
- Create: `scripts/verifyReleaseEvidence.mjs`
- Modify: `.github/workflows/ci.yml`

**Changes**

- Set `package.json` version to `0.1.0`.
- Add `npm run release:check`.
- Verify the package version is a non-placeholder semver.
- Verify the package/lock names agree and let `npm ci` enforce dependency-lock integrity.
- Read `CURRENT_GAME_SCHEMA_VERSION` directly from `GameState.ts`.
- Require the matching `docs/releases/v0.1.0.md` decision record and its mandatory sections.
- Require verify, critical/full E2E, smoke/long soak, and release-gate scripts.
- Run the fast release metadata check in normal PR/main quality CI after `npm run verify` and before soak smoke.

**Verification**

- No network is required for `release:check`.
- Feature pushes still do not trigger CI.
- PR/main CI retains dependency audit, full repository verify, smoke soak, and full mobile E2E.

## Task 2 — v0.1.0 release decision record

**Files**

- Create: `docs/releases/v0.1.0.md`

**Required evidence**

- PR18-1 main CI `34732296055` GREEN.
- PR18-2 long-run CI `34769879886` GREEN for 1/10/30-season release seeds with zero hard invariant failures.
- PR18-2 main CI `34771565962` GREEN.
- PR18-3 PR CI `34786414115` GREEN with 92/92 mobile E2E.
- PR18-3 main CI `34786794805` GREEN.
- Phase18 long-run report: `docs/superpowers/reports/2026-09-14-phase18-pr18-2-soak-balance-results.md`.

**Release record contents**

- Release notes.
- Required release evidence and blocking rules.
- Node/Cloudflare/Supabase environment review.
- Database migration review through `202609100001_phase16_pvp_match_sessions.sql`.
- Exact `Release version: 0.1.0` and `Save schema: v8` markers.
- Evidence-backed non-blocking Phase19 balance/progression targets.
- Rollback to pre-PR18-4 known-good SHA `2f21c00b40bda57019b322a90e119d7e92aa2c3b` without destructive DB migration rollback.
- Exact merge -> main CI -> manual Release Gate -> tag sequence.

## Task 3 — Manual-only final Release Gate

**Files**

- Create: `scripts/runReleaseGate.mjs`
- Create: `.github/workflows/release-gate.yml`
- Modify: `package.json`

**Changes**

Add `npm run release:gate` that runs sequentially:

1. `npm run release:check`
2. `npm run verify`
3. `npm run soak:smoke`
4. `npm run soak:long` when `COURT_LEGACY_RELEASE_LONG_SOAK=1`
5. `npm run test:e2e`

The full E2E suite already contains all `@critical` journeys, so the release gate does not run the critical subset twice.

Create a `Release Gate` GitHub Actions workflow with only `workflow_dispatch`. It must:

- use Node from `.node-version`;
- install with `npm ci`;
- run the same retrying production dependency audit as normal CI;
- install Chromium;
- run the release gate with long soak enabled;
- use `contents: read` permissions;
- avoid `cancel-in-progress` for an intentionally dispatched release run.

## Task 4 — PR, merge, and tag-ready verification

**Diff review**

- No gameplay/domain/PvP production behavior changes.
- No save schema change.
- No database migration.
- No feature-push CI trigger.

**Required PR checks**

- dependency-audit GREEN;
- `npm run verify` GREEN;
- `npm run release:check` GREEN;
- soak smoke GREEN;
- full mobile E2E GREEN.

**Completion sequence**

1. Open Draft PR `release: prepare Court Legacy v0.1.0`.
2. Investigate exact logs for any RED result; do not rerun blindly.
3. Mark Ready only after the exact PR head is fully GREEN.
4. Merge with the repository's normal merge-commit strategy.
5. Confirm post-merge `main` CI is fully GREEN for the merge SHA.
6. Manually run the `Release Gate` on the exact verified `main` SHA.
7. Only then create tag/release `v0.1.0`.
