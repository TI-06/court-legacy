# Phase20 PR20-1 Pre-CI Review

Date: 2026-09-14

## Scope

- Derived head-to-head records from bounded historical match summaries.
- Added destiny-rival / rivalry / nemesis / revenge / visible streak labels.
- Added notable-match ranking using persisted match facts only.
- Added rivalry/legacy presentation to the existing School `記録` surface.
- Added compact pre-match rivalry context for local PVE opponents only.

## Boundary review

- `CURRENT_GAME_SCHEMA_VERSION` unchanged at 8.
- No persistence shape changes.
- No match simulation, player ability, economy, or rivalry-score mutation formulas changed.
- No PvP domain, worker, payload, privacy, authority, reconnect, or idempotency files changed.
- Pre-match rivalry lookup requires a single local opponent school resolvable from local player career ownership; the normal PvP privacy path has no local opponent selection and therefore renders no rivalry context.
- Historical notable ranking does not infer old strength/reputation from current school state.

## Static review fixes before CI

- Corrected the School legacy UI test so the `天敵` fixture actually satisfies the approved <=25% user win-rate threshold (1-3 rather than 2-3).
- Kept existing large `SchoolScreen` and `GameApp` files unchanged by integrating through existing presentation/component boundaries.

## CI strategy

The sandbox cannot resolve github.com for a local clone, so executable repository verification is unavailable locally in this session. Official PR CI is therefore the first executable gate. The PR is opened only after the above static/type-boundary review to minimize failed CI runs and notification noise.
