# Deployment refresh — 2026-09-04

This documentation-only commit intentionally triggers a fresh deployment from `main` after the production Supabase secret was configured in Cloudflare.

Runtime code is unchanged.
