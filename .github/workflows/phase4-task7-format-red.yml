name: Phase4 Task7 format RED

on:
  push:
    branches:
      - feature/court-legacy-v2-phase4-async-pvp
    paths:
      - .github/workflows/phase4-task7-format-red.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/court-legacy-v2-phase4-async-pvp
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci
      - run: npx prettier tests/unit/features/pvp/PvpScreen.test.tsx tests/unit/app/GameApp.pvp.test.tsx --write
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/unit/features/pvp/PvpScreen.test.tsx tests/unit/app/GameApp.pvp.test.tsx
          git commit -m "style: format PvP UI RED tests"
          git push origin HEAD:feature/court-legacy-v2-phase4-async-pvp
