name: Phase 12 E2E Align

on:
  push:
    branches: [feature/phase12-game-loop-polish]
    paths:
      - scripts/phase12-e2e-align.py
      - .github/workflows/phase12-e2e-align.yml

permissions:
  contents: write

jobs:
  align:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/phase12-game-loop-polish
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci
      - name: Apply Phase 12 E2E alignment
        run: python scripts/phase12-e2e-align.py
      - name: Format changed code
        run: npm run format
      - name: Verify quality
        run: npm run verify
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Verify mobile E2E
        run: npm run test:e2e
      - name: Upload Playwright diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: phase12-e2e-alignment-report
          path: |
            playwright-report
            test-results
          retention-days: 7
      - name: Commit aligned Phase 12 E2E and cleanup
        run: |
          rm -f scripts/phase12-e2e-align.py .github/workflows/phase12-e2e-align.yml
          git config user.name "github-actions[bot]"
          git config user.email "418982982+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "test: align mobile E2E with Phase 12 navigation"
          git push origin HEAD:feature/phase12-game-loop-polish
