name: Task 9 Shell RED Check

on:
  push:
    branches:
      - feature/court-legacy-v2-phase1
    paths:
      - tests/unit/ui/shell/GameHeader.test.tsx
      - tests/unit/features/more/MoreScreen.test.tsx
      - .github/workflows/task8-red-temp.yml

permissions:
  contents: read

jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci
      - name: Run focused Task 9 shell tests
        run: npm test -- tests/unit/ui/shell/GameHeader.test.tsx tests/unit/features/more/MoreScreen.test.tsx
