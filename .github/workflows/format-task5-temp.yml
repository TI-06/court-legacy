name: Temporary Task 5 Formatter

on:
  push:
    branches:
      - feature/court-legacy-v2-phase1
    paths:
      - .github/workflows/format-task5-temp.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci
      - name: Format Task 5 files
        run: >-
          npx prettier
          tests/unit/worker/applyGameAction.test.ts
          tests/unit/worker/gameAction.test.ts
          tests/unit/worker/onboarding.test.ts
          worker/data/SupabaseGameStore.ts
          worker/routes/gameAction.ts
          --write
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            echo "No formatting changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/unit/worker/applyGameAction.test.ts tests/unit/worker/gameAction.test.ts tests/unit/worker/onboarding.test.ts worker/data/SupabaseGameStore.ts worker/routes/gameAction.ts
          git commit -m "style: format server-authoritative game actions"
          git push
