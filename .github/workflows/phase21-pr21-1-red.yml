name: Phase21 PR21-1 Expected RED

on:
  push:
    branches:
      - feat/phase21-1-fullscreen-events-personality

permissions:
  contents: read

jobs:
  expected-red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --fund=false
      - name: Verify personality presentation test is RED
        shell: bash
        run: |
          set +e
          npx vitest run tests/unit/domain/player/playerPersonalityPresentation.test.ts
          status=$?
          set -e
          if [ "$status" -eq 0 ]; then
            echo "Expected RED, but personality presentation test passed unexpectedly."
            exit 1
          fi
          echo "Expected RED confirmed; workflow remains green by design."
