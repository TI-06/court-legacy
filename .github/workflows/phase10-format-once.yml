name: Phase10 Format Once

on:
  push:
    branches:
      - feature/phase10-training-player-mobile-ui
    paths:
      - .github/workflows/phase10-format-once.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/phase10-training-player-mobile-ui
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Format Task 1 files
        run: >-
          npx prettier --write
          src/domain/notifications/gameNotifications.ts
          src/domain/model/GameState.ts
          src/domain/generation/generateWorld.ts
          tests/unit/domain/notifications/gameNotifications.test.ts
      - name: Commit formatting if needed
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/domain/notifications/gameNotifications.ts src/domain/model/GameState.ts src/domain/generation/generateWorld.ts tests/unit/domain/notifications/gameNotifications.test.ts
          if ! git diff --cached --quiet; then
            git commit -m "style: format phase10 notification task"
            git push origin HEAD:feature/phase10-training-player-mobile-ui
          fi
