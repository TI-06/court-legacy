name: Phase14 Player Hub temporary formatter

on:
  push:
    branches:
      - feat/phase14-player-hub-ui

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/phase14-player-hub-ui
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci --no-audit --fund=false
      - run: npx prettier docs/superpowers/plans/2026-09-09-phase14-player-hub-ui.md tests/unit/features/team/playerHubRoster.test.ts --write
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/superpowers/plans/2026-09-09-phase14-player-hub-ui.md tests/unit/features/team/playerHubRoster.test.ts
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "style: format Phase 14 Player Hub files"
          git push origin HEAD:feat/phase14-player-hub-ui
