name: Phase16 temporary formatter

on:
  push:
    branches:
      - feat/phase16-match-command-ux

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/phase16-match-command-ux
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci --no-audit --fund=false
      - run: npx prettier tests/unit/app/Phase16GameAppMatchCommand.test.tsx --write
      - name: Commit formatting and remove temporary workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/unit/app/Phase16GameAppMatchCommand.test.tsx
          git rm .github/workflows/phase16-format-temp.yml
          git commit -m "chore: format Phase16 GameApp test"
          git push origin HEAD:feat/phase16-match-command-ux
