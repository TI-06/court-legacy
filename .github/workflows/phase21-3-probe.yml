name: Phase21-3 Task2 Red Probe

on:
  push:
    branches:
      - tmp/phase21-3-hidden-character-traits

permissions:
  contents: read

jobs:
  task2-red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --fund=false
      - name: Run Task2 assignment test
        run: npx vitest run tests/unit/domain/player/characterTraitAssignment.test.ts
