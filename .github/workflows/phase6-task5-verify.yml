name: Phase 6 Task 5 Verify

on:
  push:
    branches:
      - feature/court-legacy-v2-phase6-official-tournaments

permissions:
  contents: write

jobs:
  verify-task5:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/court-legacy-v2-phase6-official-tournaments
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci
      - name: Format Task 5 files
        run: >-
          npx prettier
          src/domain/tournament/materializeGuestOpponent.ts
          tests/unit/domain/tournament/materializeGuestOpponent.test.ts
          --write
      - name: Verify Task 5 focused behavior
        run: |
          npm test -- tests/unit/domain/tournament/materializeGuestOpponent.test.ts
          npm run typecheck
      - name: Commit formatting if needed
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/domain/tournament/materializeGuestOpponent.ts tests/unit/domain/tournament/materializeGuestOpponent.test.ts
          git commit -m "chore: finalize Phase 6 Task 5 files"
          git push origin HEAD:feature/court-legacy-v2-phase6-official-tournaments
