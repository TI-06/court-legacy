name: Phase14 Saved Lineup temporary formatter

on:
  push:
    branches:
      - feat/phase14-saved-lineup-ux

permissions:
  contents: write

# Temporary formatter for PR14-3. Remove before merge.
jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/phase14-saved-lineup-ux
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci --no-audit --fund=false
      - run: npx prettier . --write
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "style: format Phase 14 saved lineup changes"
          git push origin HEAD:feat/phase14-saved-lineup-ux
