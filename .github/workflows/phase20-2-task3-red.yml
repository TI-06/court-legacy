name: Phase20-2 Task3 RED

on:
  push:
    branches:
      - "feature/phase20-2-player-concern-guidance"

permissions:
  contents: read

jobs:
  notification-red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci --no-audit --fund=false
      - run: npx vitest run tests/unit/domain/notifications/phase20ConcernNotifications.test.ts tests/unit/persistence/phase20ConcernNotificationCodec.test.ts
